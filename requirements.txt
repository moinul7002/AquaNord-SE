# AquaNord-SE — Python dependencies
# Tested on Python 3.11+

# Core data
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
scipy>=1.11

# Geospatial
geopandas>=1.0
pyproj>=3.6
netCDF4>=1.6
xarray>=2024.1
zarr>=2.18
rasterio>=1.3

# Machine learning
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
torch>=2.2               # GPU optional; CPU fallback is automatic

# Visualisation
matplotlib>=3.8
cartopy>=0.22            # optional — required only for map figures

# Data access
cdsapi>=0.7              # ERA5 via Copernicus CDS
earthengine-api>=0.1.390 # GEE satellite extraction
requests>=2.31
python-dotenv>=1.0

# Meteorological
cfgrib>=0.9              # GRIB decoding for ERA5 downloads
